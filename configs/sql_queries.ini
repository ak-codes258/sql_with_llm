[queries]
query =                 SELECT * 
                FROM test_data_event_store
                WHERE payment_status = 'COMPLETED' 
                AND sender_transaction_amount > 500 
                AND TIMESTAMP_MILLIS(created_at) >= TIMESTAMP_SUB(CURRENT_TIMESTAMP(), INTERVAL 90 DAY)